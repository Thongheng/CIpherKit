{
  "SHA-1": {
    "requires_key": false,
    "code": "def generate(payload, passcode=None, custom_data=None, key_order=None):\n    import hashlib\n\n    debug_log = \"--- HASHING PROCESS DEBUG ---\\n\"\n    debug_log += \"Algorithm: SHA-1 (plain, no key)\\n\"\n\n    if key_order:\n        keys_to_sign = key_order\n        debug_log += \"[1] Using Custom Keys Order: \" + str(keys_to_sign) + \"\\n\"\n    else:\n        keys_to_sign = [k for k in payload.keys() if k != 'hash']\n        debug_log += \"[1] Using Extracted Keys Order: \" + str(keys_to_sign) + \"\\n\"\n\n    concat_str = \"\"\n    debug_log += \"\\n[2] Concatenating Values:\\n\"\n    for k in keys_to_sign:\n        val = payload.get(k)\n        if val is None: val = \"\"\n        if k in ('amount', 'commission'):\n            try:\n                val = \"{:.2f}\".format(float(val))\n            except:\n                pass\n        debug_log += \"    - {} = '{}'\\n\".format(k, val)\n        concat_str += str(val)\n\n    debug_log += \"\\n[3] Final Concatenated String:\\n    -> '{}'\\n\".format(concat_str)\n    debug_log += \"\\n[4] Generating SHA-1 hash...\\n\"\n\n    digest = hashlib.sha1(concat_str.encode('utf-8')).hexdigest()\n    debug_log += \"\\n[5] Result:\\n    -> {}\\n\".format(digest)\n    return digest, debug_log",
    "description": "SHA-1: Plain SHA-1 hash of concatenated payload values. No secret key required."
  }
}